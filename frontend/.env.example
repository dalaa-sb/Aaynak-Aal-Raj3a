# Frontend environment variables (Vite picks up VITE_* automatically)
# Copy to .env and fill in values. Never commit the real .env.

VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
